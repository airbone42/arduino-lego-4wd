// Template for the WiFi credentials of the ESP32 gamepad bridge.
// -> Copy this file to "arduino_secrets.h" (same folder) and fill in your
//    own WiFi name and password.
// The real "arduino_secrets.h" is excluded via .gitignore and never ends
// up in the repository.
//
// The ESP32 only needs the WiFi for over-the-air updates - driving works
// without it. That matters: once the board is powered from VIN you must
// not plug in USB any more, so OTA is the only way to reach it.

#define SECRET_SSID "YOUR_WIFI_NAME"
#define SECRET_PASS "YOUR_WIFI_PASSWORD"

// Password for the over-the-air firmware upload (OTA). Pick anything you
// like, you just have to pass the same value when uploading.
#define SECRET_OTA_PASS "lego4wd"
